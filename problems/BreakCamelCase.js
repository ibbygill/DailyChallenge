function solution(string) {
    let splitStr = string.split("");
    let newStr = string.split("");
    let capStr = string.toUpperCase().split("");
    for (i = splitStr -1; i >= 0; i--) {
        if(){

        }
    }
    return newStr.join("");
}

// still a work in progress